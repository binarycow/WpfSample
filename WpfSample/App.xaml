<Application x:Class="WpfSample.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfSample"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type local:CharacterSheet}">
             <local:CharacterSheetControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:Character}">
            <local:CharacterControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:AttributeBlock}">
            <local:AttributeBlockControl />
        </DataTemplate>
    </Application.Resources>
</Application>
